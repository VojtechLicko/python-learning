age: int = 20

print(age)
age = "je to tak"
print(age)
# newly from python 3.6 u can set the data type of something
age = age + " není to tak"
print(id(age))
# u can reverse strings with this simple trick
print(age[::-1])
