coordinates = (4, 5)
# Tuples are immutable u cant change them
print(coordinates[0])
# You use tuples for data that is never going to change
# Tuples are ordered
# Tuples allow duplicates
print(coordinates.count(4))
coordinates[0] = "string"  # tuples are immutable so this code throws tuple' object does not support item assignment





























