# with python u can unpack lists or tuples etc. into multiple variables
# and u can use asterisk(*) to unpack multiple values as a list into one variable

my_list = [1, 2, 3, 4, 8]
x, y, *z = my_list
print(x)
print(y)
print(z)
